apiVersion: v1
kind: Service
metadata:
  name: {{ include "springboot-crud-mysql-vuejs-helm.fullname" . }}
  labels:
    {{- include "springboot-crud-mysql-vuejs-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springboot-crud-mysql-vuejs-helm.selectorLabels" . | nindent 4 }}
